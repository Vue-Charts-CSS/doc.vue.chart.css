language: node_js
node_js:
  - lts/*
install:
  - yarn --frozen-lockfile
script:
  - yarn run build
deploy:
  provider: pages
  skip_cleanup: true
  local_dir: src/.vuepress/dist
  github_token: $GITHUB_TOKEN
  keep_history: true
  on:
    branch: main
